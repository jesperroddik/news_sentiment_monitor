# Copy to .env and fill in your own credentials.

# Neon serverless PostgreSQL. Must include sslmode=require.
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require

# DeepL free tier API key (500,000 chars/month).
DEEPL_API_KEY=your-deepl-free-api-key

# NewsAPI.org key (optional — RSS is the primary source and needs no key).
NEWS_API_KEY=your-newsapi-org-key
